<h1>Books</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Pages</th>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= link_to book.name, "/books/#{book.id}" %></td>
    <td><%= book.pages %></td>
  </tr>
  <% end %>
</table>