<h1><%= @book.name %></h1>

<%= link_to "Edit", "/books/#{@book.id}/edit" %> | 
<%= link_to "Delete", "/books/#{@book.id}", method: :delete, data:{confirm: "Are you sure?"} %>
<p>Pages: <%= @book.pages %></p>